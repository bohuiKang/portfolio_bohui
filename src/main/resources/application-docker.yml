spring:
  jpa:
    database: mysql
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: false
        default_batch_fetch_size: 10
  datasource:
    url: jdbc:mysql://mysql:3307/portfolio
    username: root
    password: ENC(yOcM+ixMMTqmLYzTA1nIClUFs7r0ZAlP)
    driver-class-name: com.mysql.cj.jdbc.Driver